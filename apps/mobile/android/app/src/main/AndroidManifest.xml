<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <service
            android:name=".PiecesAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Part 1: system-wide notification listener. Granted via
             `cmd notification allow_listener` or the settings screen; inert
             until granted AND notification_capture_enabled is on. -->
        <service
            android:name=".NotificationCaptureService"
            android:label="Pieces Notification Capture"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Persistent background sync job to drain outbox across process restarts -->
        <service
            android:name=".TelemetrySyncJobService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Runtime-requested via BarcodeScanner.requestPermissions() in Setup's
         "Scan to Connect" flow — declaring it here just makes it requestable. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Needed for AccessibilityScanner.listInstalledApps() (the app picker for
         screen-capture allowlisting) to see the full installed-app list on
         Android 11+, which restricts package visibility by default. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <!-- Declarative only — this is an AppOps special permission with no runtime
         dialog; actual grant happens via Settings.ACTION_USAGE_ACCESS_SETTINGS
         (see AccessibilityPlugin.openUsageAccessSettings). Declaring it here is
         standard practice for clarity even though the manifest entry alone
         grants nothing. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <!-- Part 2: full SMS/MMS bodies + history backfill. Declared so they are
         grantable via the app details settings screen (SmsPlugin.openAppSettings)
         or the Android 14+ restricted-settings flow — no standard runtime
         dialog for a non-default-SMS app. Inert until granted. READ_CONTACTS
         is for the contact-allowlist picker (only messages from ticked
         contacts are captured). -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
</manifest>
